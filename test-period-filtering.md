# تست قابلیت فیلتر کردن اهداف بر اساس دوره

## تغییرات انجام شده:

### 1. State جدید برای دنبال کردن دوره‌های انتخاب شده
- اضافه شدن `selectedPeriods` که دوره‌های انتخاب شده برای هر نوع هدف را ذخیره می‌کند
- مقدار پیش‌فرض: دوره‌های فعلی (سال جاری، فصل جاری، ماه جاری، هفته جاری)

### 2. به‌روزرسانی تابع `loadAllGoals`
- حالا از `selectedPeriods` استفاده می‌کند به جای `getCurrentPeriod`
- این یعنی فقط اهداف مربوط به دوره‌های انتخاب شده بارگذاری می‌شوند

### 3. به‌روزرسانی `handlePeriodSelect`
- وقتی کاربر یک دوره را از تقویم انتخاب می‌کند، `selectedPeriods` به‌روز می‌شود
- بعد از به‌روزرسانی، اهداف دوباره بارگذاری می‌شوند (به خاطر dependency در useCallback)

### 4. UI جدید
- **دکمه "Current"**: فقط زمانی نمایش داده می‌شود که دوره‌های انتخاب شده با دوره‌های فعلی متفاوت باشند
- **نمایشگر دوره‌ها**: یک بنر در بالای لیست اهداف که نشان می‌دهد کدام دوره‌ها در حال نمایش هستند
- **عنوان هر بخش**: حالا دوره انتخاب شده را در کنار نام نوع هدف نشان می‌دهد

## نحوه تست:

### تست 1: نمایش دوره فعلی (پیش‌فرض)
1. صفحه را باز کنید
2. باید اهداف دوره فعلی را ببینید:
   - سال: 2025
   - فصل: Q4 2025 (دسامبر = فصل چهارم)
   - ماه: December 2025
   - هفته: هفته فعلی
3. دکمه "Current" نباید نمایش داده شود

### تست 2: انتخاب یک فصل خاص
1. روی "📅 Select Period" کلیک کنید
2. یک سال را انتخاب کنید (مثلاً 2024)
3. روی Q1 کلیک کنید
4. باید:
   - فقط اهداف Q1 2024 در بخش Quarterly نمایش داده شود
   - دکمه "🔄 Current" ظاهر شود
   - یک بنر نمایش داده شود که نشان می‌دهد Q1 2024 انتخاب شده است

### تست 3: انتخاب یک ماه خاص
1. روی "📅 Select Period" کلیک کنید
2. یک سال را انتخاب کنید
3. روی یک ماه کلیک کنید (مثلاً January)
4. باید:
   - فقط اهداف January آن سال در بخش Monthly نمایش داده شود
   - دیگر بخش‌ها همچنان دوره‌های قبلی خود را نشان می‌دهند

### تست 4: بازگشت به دوره فعلی
1. بعد از انتخاب یک دوره غیر فعلی
2. روی "🔄 Current" کلیک کنید
3. باید:
   - همه بخش‌ها به دوره‌های فعلی برگردند
   - دکمه "Current" ناپدید شود

### تست 5: افزودن هدف برای یک دوره خاص
1. یک فصل خاص را انتخاب کنید (مثلاً Q2 2024)
2. روی "➕ Add Goal" کلیک کنید
3. نوع را Quarterly انتخاب کنید
4. باید ببینید که:
   - فیلد Quarter روی Q2 تنظیم شده است
   - Year روی 2024 تنظیم شده است
5. هدف را ذخیره کنید
6. باید هدف جدید در لیست Quarterly Goals نمایش داده شود

## نتیجه:

با این تغییرات:
- ✅ در بخش سال فقط اهداف آن سال نمایش داده می‌شود
- ✅ در بخش فصل فقط اهداف آن فصل نمایش داده می‌شود
- ✅ در بخش ماه فقط اهداف آن ماه نمایش داده می‌شود
- ✅ در بخش هفته فقط اهداف آن هفته نمایش داده می‌شود
- ✅ کاربر می‌تواند دوره‌های مختلف را انتخاب کند و فقط اهداف آن دوره‌ها را ببیند
- ✅ کاربر می‌تواند به راحتی به دوره‌های فعلی برگردد
