# 📱 راهنمای دیباگ موبایل | Mobile Debug Guide

## مشکل: صفحه سفید با دکمه چرخ دنده

اگر در موبایل فقط صفحه سفید و یک دکمه 🐛 می‌بینید:

---

## ✅ راه‌حل سریع | Quick Fix

### گام 1: روی دکمه 🐛 بزنید
```
دکمه چرخ دنده (🐛) در گوشه پایین سمت چپ
↓
کنسول دیباگ باز می‌شود
↓
خطاها را می‌بینید!
```

### گام 2: خطا را بخوانید
خطاهای رایج:
- `localStorage is not defined` → SSR مشکل دارد
- `context is undefined` → مشکل Provider
- `Cannot read property` → کامپوننت نمونت نشده

### گام 3: Screenshot بگیرید و ارسال کنید
از کنسول دیباگ عکس بگیرید تا من ببینم مشکل چیست.

---

## 🔧 راه‌حل‌های رایج

### مشکل 1: خطای localStorage

**خطا:**
```
ReferenceError: localStorage is not defined
```

**راه‌حل:**
این مشکل را حل کردیم! اگر هنوز می‌بینید:

1. Ctrl+F5 (Hard Refresh) بزنید
2. یا Cache مرورگر را پاک کنید:
   - در Chrome: سه نقطه → Settings → Privacy → Clear browsing data
   - "Cached images and files" را انتخاب کنید
   - Clear data

### مشکل 2: خطای Context

**خطا:**
```
Cannot read property 'currentUser' of undefined
```

**راه‌حل:**
1. در Debug Console روی دکمه "Clear Data" بزنید
2. صفحه را Reload کنید

### مشکل 3: صفحه خالی بدون خطا

**راه‌حل:**
1. دکمه 🐛 را بزنید
2. اگر خطایی نبود، از Chrome Remote Debugging استفاده کنید

---

## 🌐 Chrome Remote Debugging

### برای Android:

**در گوشی:**
1. تنظیمات → درباره تلفن
2. 7 بار روی "شماره ساخت" بزنید (Developer Mode فعال می‌شود)
3. تنظیمات → گزینه‌های توسعه‌دهنده
4. "USB debugging" را فعال کنید
5. گوشی را به کامپیوتر وصل کنید

**در کامپیوتر (Chrome):**
1. باز کنید: `chrome://inspect/#devices`
2. گوشی شما را پیدا کنید
3. روی "inspect" کلیک کنید
4. Console را ببینید

### برای iOS (با Mac):

**در iPhone:**
1. تنظیمات → Safari → پیشرفته
2. "Web Inspector" را فعال کنید

**در Mac (Safari):**
1. Safari → Preferences → Advanced
2. "Show Develop menu" را فعال کنید
3. Develop → [Your iPhone] → [صفحه]
4. Console را ببینید

---

## 🐛 استفاده از Debug Console

### دکمه‌های موجود:

```
┌──────────────────────────────────────────┐
│  🐛 Mobile Debug Console   [🗑️ Clear] [✕] │
├──────────────────────────────────────────┤
│  [ERROR] خطا اینجا نمایش...             │
│  [WARN] هشدار اینجا نمایش...             │
│  [INFO] اطلاعات سیستم...                 │
└──────────────────────────────────────────┘
```

**دکمه Close (✕):**
- کنسول را می‌بندد (خطاها همچنان ذخیره می‌شوند)

**دکمه Clear (🗑️):**
- تمام لاگ‌ها را پاک می‌کند

**دکمه 🐛 (شناور):**
- کنسول را باز/بسته می‌کند
- فقط در موبایل (width < 768px) نمایش داده می‌شود
- **در هر دو حالت development و production فعال است**

---

## 📸 نحوه گرفتن Screenshot

### Android:
```
Power + Volume Down (همزمان)
یا
Power → Screenshot
```

### iOS:
```
Side Button + Volume Up (همزمان)
یا
Home + Power (مدل‌های قدیمی)
```

---

## 🔍 خطاهای شناخته شده

### 1. Hydration Mismatch

**خطا:**
```
Warning: Text content did not match. Server: "..." Client: "..."
```

**دلیل:**
- تفاوت بین SSR و Client render
- معمولاً به خاطر Date, localStorage, window

**راه‌حل:**
```typescript
// استفاده از useEffect
const [mounted, setMounted] = useState(false);

useEffect(() => {
  setMounted(true);
}, []);

if (!mounted) return null;
```

### 2. Module Not Found

**خطا:**
```
Module not found: Can't resolve '@/...'
```

**دلیل:**
- Path alias مشکل دارد
- فایل وجود ندارد

**راه‌حل:**
1. بررسی tsconfig.json
2. بررسی وجود فایل
3. Restart dev server

### 3. Maximum Update Depth

**خطا:**
```
Maximum update depth exceeded
```

**دلیل:**
- حلقه بی‌نهایت در useState/useEffect

**راه‌حل:**
1. dependency array را بررسی کنید
2. setState را خارج از render بگذارید

---

## 💡 نکات مفید

### نکته 1: Force Refresh
```
موبایل: حذف App از Recent Apps + باز کردن مجدد
Browser: Ctrl+F5 یا Cmd+Shift+R
```

### نکته 2: پاک کردن Cache
```javascript
// در Debug Console روی "Clear Data" بزنید
// یا Manual:
localStorage.clear();
sessionStorage.clear();
location.reload();
```

### نکته 3: بررسی Network
```
در Chrome Remote Debugging:
Network Tab → ببینید کدام فایل‌ها لود نمی‌شوند
```

---

## 🆘 اگر هیچ کدام کار نکرد

### گزینه‌های باقی‌مانده:

1. **مرورگر دیگری امتحان کنید:**
   - Chrome → Firefox
   - Safari → Edge
   - Samsung Internet → Chrome

2. **Incognito/Private Mode:**
   - Extensions غیرفعال می‌شوند
   - Cache تازه است

3. **WiFi را بررسی کنید:**
   - آیا در همان شبکه هستید؟
   - IP صحیح است؟ (172.17.100.69)
   - Port 3001 باز است?

4. **Firewall:**
   - آیا Firewall port 3001 را block می‌کند?
   - آیا antivirus مشکل ایجاد می‌کند?

---

## 📞 ارسال گزارش خطا

اگر مشکل حل نشد، این اطلاعات را ارسال کنید:

### اطلاعات مورد نیاز:

```
1. Screenshot از Debug Console (🐛)
2. نوع گوشی: (مثلاً: Samsung Galaxy S21)
3. نسخه Android/iOS: (مثلاً: Android 12)
4. مرورگر: (Chrome, Safari, Firefox, ...)
5. نسخه مرورگر: (مثلاً: Chrome 120)
6. خطای دقیق: (کپی از Console)
```

### نحوه ارسال:

1. روی دکمه 🐛 بزنید
2. Screenshot بگیرید
3. همراه با اطلاعات بالا ارسال کنید

---

## ✅ Checklist دیباگ

قبل از ارسال گزارش، این موارد را بررسی کنید:

- [ ] دکمه 🐛 را زدم و Console را دیدم
- [ ] Screenshot گرفتم
- [ ] Cache را پاک کردم (Ctrl+F5)
- [ ] localStorage را پاک کردم (Clear Data)
- [ ] مرورگر دیگری امتحان کردم
- [ ] در همان WiFi هستم
- [ ] IP و Port صحیح است (172.17.100.69:3001)
- [ ] Chrome Remote Debugging را امتحان کردم

---

## 🎯 مثال واقعی

### مشکل:
```
صفحه سفید + دکمه 🐛
```

### راه‌حل:
```
1. روی 🐛 زدم
   ↓
2. خطا دیدم:
   [ERROR] localStorage is not defined at ai-context.ts:59

3. این خطا را رفع کردیم!
   ↓
4. Ctrl+F5 زدم
   ↓
5. کار کرد! ✅
```

---

**اگر بعد از تمام این مراحل باز مشکل دارید، Screenshot از 🐛 Console را ارسال کنید!** 📸

---

---

## 🔄 آخرین تغییرات | Latest Changes (2025-12-28)

### نسخه 1.1.0:
- ✅ Debug Console در هر دو حالت dev و production فعال شد
- ✅ دکمه Clear اضافه شد برای پاک کردن لاگ‌ها
- ✅ طراحی بهتر با header و دکمه‌های واضح‌تر
- ✅ نمایش اطلاعات اولیه سیستم (Device, Window size, DOM state)
- ✅ رنگ‌بندی بهتر برای ERROR (قرمز)، WARN (نارنجی)، INFO (آبی)

**Last Updated**: 2025-12-28
**Version**: 1.1.0
